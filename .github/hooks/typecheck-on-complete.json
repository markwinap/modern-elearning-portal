{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "npx tsc --noEmit 2>&1 | head -50",
        "windows": "npx tsc --noEmit 2>&1 | Select-Object -First 50",
        "timeout": 60
      }
    ]
  }
}
