{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "pnpm lint 2>&1 | head -30",
        "windows": "pnpm lint 2>&1 | Select-Object -First 30",
        "timeout": 60
      }
    ]
  }
}
